# Runtime dependencies for the local UI.
#
# PyTorch is installed separately by setup_ui_cpu.* so CPU-only users do not
# accidentally install a CUDA wheel from a project requirements file.
Pillow==12.2.0
